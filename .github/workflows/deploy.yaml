name: Build and Package MobiSync

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version Number (e.g., 4.6.1)'
        required: true
        default: '1.0.0'

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
    - name: Checkout Code
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

    - name: Setup .NET
      uses: actions/setup-dotnet@39502139a996e757446281726242b7cfca62e6c0 # v4.3.0
      with:
        dotnet-version: '10.0.x'

    - name: Restore Dependencies
      run: dotnet restore ./Komandio.Tools.Mobisync/Komandio.Tools.Mobisync.csproj

    - name: Publish Single File
      run: |
        dotnet publish ./Komandio.Tools.Mobisync/Komandio.Tools.Mobisync.csproj `
          -c Release `
          -r win-x64 `
          --self-contained true `
          -p:PublishSingleFile=true `
          -p:IncludeNativeLibrariesForSelfExtract=true `
          -p:Version=${{ github.event.inputs.version }} `
          -o ./publish

    - name: Rename and Prepare Binary
      run: |
        Rename-Item "./publish/MobiSync.exe" "mobisync_x64_${{ github.event.inputs.version }}.exe"

    - name: Create Release
      uses: softprops/action-gh-release@c95fe1489396fe8a219672d03a19bb51ad127304 # v2.2.1
      with:
        tag_name: v${{ github.event.inputs.version }}
        name: Release v${{ github.event.inputs.version }}
        files: ./publish/mobisync_x64_${{ github.event.inputs.version }}.exe
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
