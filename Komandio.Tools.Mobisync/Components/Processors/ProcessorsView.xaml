<UserControl x:Class="Komandio.Tools.Mobisync.Components.Processors.ProcessorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:processors="clr-namespace:Komandio.Tools.Mobisync.Components.Processors"
             d:DataContext="{d:DesignInstance processors:ProcessorsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="8*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: List & Guide -->
        <Grid Grid.Column="0" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="390" />
            </Grid.RowDefinitions>
            <processors:RuleListView Grid.Row="0" />
            <processors:RegexGuideView Grid.Row="1" Margin="0,24,0,0"/>
        </Grid>

        <!-- Right: Editor -->
        <processors:RuleEditorView Grid.Column="1"/>
    </Grid>
</UserControl>
