<UserControl x:Class="Komandio.Tools.Mobisync.Components.Vehicles.VehiclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vehicles="clr-namespace:Komandio.Tools.Mobisync.Components.Vehicles"
             d:DataContext="{d:DesignInstance vehicles:VehiclesViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Active Vehicle Card -->
        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="VehicleBus24" FontSize="48" Foreground="{StaticResource AccentBlueBrush}" Margin="0,0,24,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding ActiveVehicleLabel}" FontSize="12" FontWeight="Black" Foreground="{StaticResource TextSlateBrush}"/>
                    <TextBlock Text="{Binding ActiveVehicle}" FontSize="28" FontWeight="Black" Foreground="White" Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding ActiveVehicleStatus}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentBlueBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="VEHICLE ACTIVITY LOG" Style="{StaticResource ModuleTitleStyle}" VerticalAlignment="Center" Margin="0"/>
                        <Grid Margin="32,0,0,0" VerticalAlignment="Center">
                            <ui:TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                                        PlaceholderText="Filter vehicles..."
                                        Width="200" Height="24" FontSize="9" Padding="8,0,8,0"
                                        Icon="{ui:SymbolIcon Search24}"
                                        Background="#1a000000" BorderBrush="{StaticResource BorderBrush}"/>
                        </Grid>
                    </StackPanel>
                </Grid>
                <ListView Grid.Row="1" ItemsSource="{Binding FilteredEvents}" 
                          ItemTemplate="{StaticResource EventRowTemplate}"
                          Background="Transparent" BorderThickness="0"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          VirtualizingStackPanel.ScrollUnit="Pixel"
                          VirtualizingStackPanel.CacheLengthUnit="Item"
                          VirtualizingStackPanel.CacheLength="10,10"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
