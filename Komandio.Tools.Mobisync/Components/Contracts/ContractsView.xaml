<UserControl x:Class="Komandio.Tools.Mobisync.Components.Contracts.ContractsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:Komandio.Tools.Mobisync.Models"
             xmlns:contracts="clr-namespace:Komandio.Tools.Mobisync.Components.Contracts"
             d:DataContext="{d:DesignInstance contracts:ContractsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="800"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Stats Header -->
        <Grid Margin="0,0,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Accepted Widget -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,12,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="ACCEPTED" Style="{StaticResource StatLabelStyle}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ActiveCount}" FontSize="24" FontWeight="Black" Foreground="{StaticResource AccentBlueBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Completed Widget -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="12,0,12,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="COMPLETED" Style="{StaticResource StatLabelStyle}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding CompletedCount}" FontSize="24" FontWeight="Black" Foreground="#4ADE80" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Failed Widget -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="12,0,0,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="FAILED" Style="{StaticResource StatLabelStyle}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding FailedCount}" FontSize="24" FontWeight="Black" Foreground="#F87171" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="1" Text="ACCEPTED CONTRACTS" Style="{StaticResource ModuleTitleStyle}" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="2">
            <StackPanel Margin="0,0,12,0">
                <!-- Active List -->
                <ListView ItemsSource="{Binding ActiveMissions}" 
                          Background="Transparent" BorderThickness="0"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:MissionModel}">
                            <Border Margin="0,0,0,12" Padding="16">
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#0dffffff" />
                                                <Setter Property="BorderBrush" Value="#1a5e5ce6" />
                                            </Trigger>
                                            <DataTrigger Binding="{Binding IsFollowed}" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}" />
                                                <Setter Property="BorderThickness" Value="2" />
                                                <Setter Property="Opacity" Value="1.0" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Border Width="40" Height="40" CornerRadius="10" Background="#1a5e5ce6"
                                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                        <ui:SymbolIcon Symbol="ClipboardTask24" Foreground="{StaticResource AccentBlueBrush}" FontSize="18" />
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                        <Grid>
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Black" Foreground="{StaticResource TextMainBrush}" />
                                            <Border x:Name="TrackingTag" CornerRadius="4" Padding="6,2" HorizontalAlignment="Right" VerticalAlignment="Top" Visibility="Collapsed" Background="#1a5e5ce6">
                                                <TextBlock Text="TRACKING" Foreground="{StaticResource AccentBlueBrush}" FontSize="10" FontWeight="Black" />
                                            </Border>
                                        </Grid>

                                        <StackPanel Margin="0,4,0,0">
                                            <TextBlock Text="{Binding CurrentObjectiveText}" FontSize="11" FontWeight="Bold" Foreground="#4ADE80" TextWrapping="Wrap">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CurrentObjectiveText}" Value="WAITING FOR DATA...">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding CurrentObjectiveText}" Value="WAITING FOR UPDATE">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>

                                        <ItemsControl ItemsSource="{Binding Objectives}" Margin="0,12,0,0">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type models:MissionObjective}">
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="12" Margin="0,0,8,0">
                                                            <ui:SymbolIcon.Style>
                                                                <Style TargetType="ui:SymbolIcon">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextSlateBrush}" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Status}" Value="COMPLETED">
                                                                            <Setter Property="Foreground" Value="#4ADE80" />
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Status}" Value="INPROGRESS">
                                                                            <Setter Property="Foreground" Value="{StaticResource AccentBlueBrush}" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </ui:SymbolIcon.Style>
                                                        </ui:SymbolIcon>
                                                        <TextBlock Grid.Column="1" Text="{Binding Text}" FontSize="11" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextSlateBrush}" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Status}" Value="INPROGRESS">
                                                                            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}" />
                                                                            <Setter Property="FontWeight" Value="Bold" />
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Status}" Value="COMPLETED">
                                                                            <Setter Property="Foreground" Value="#4ADE80" />
                                                                            <Setter Property="Opacity" Value="0.7" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="TRACKING">
                                    <Setter TargetName="TrackingTag" Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- History Section -->
                <TextBlock Text="CONTRACT HISTORY" Style="{StaticResource ModuleTitleStyle}" Margin="0,24,0,12"/>
                <ListView ItemsSource="{Binding HistoryMissions}" 
                          Background="Transparent" BorderThickness="0"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:MissionModel}">
                            <Border Margin="0,0,0,8" Padding="12" Opacity="0.6">
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#0dffffff" />
                                                <Setter Property="BorderBrush" Value="#1a5e5ce6" />
                                                <Setter Property="Opacity" Value="1.0" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <ui:SymbolIcon Symbol="History24" Foreground="{StaticResource TextSlateBrush}" FontSize="16" Margin="0,0,12,0"/>
                                    
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}" VerticalAlignment="Center"/>
                                    
                                    <Border Grid.Column="2" CornerRadius="4" Padding="8,2" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#33ffffff" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="SUCCESS">
                                                        <Setter Property="Background" Value="#404ADE80" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="FAILED">
                                                        <Setter Property="Background" Value="#40F87171" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="Black">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource TextMainBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="SUCCESS">
                                                            <Setter Property="Foreground" Value="#4ADE80" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="FAILED">
                                                            <Setter Property="Foreground" Value="#F87171" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>