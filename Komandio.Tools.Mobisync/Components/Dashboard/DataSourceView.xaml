<UserControl x:Class="Komandio.Tools.Mobisync.Components.Dashboard.DataSourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dashboard="clr-namespace:Komandio.Tools.Mobisync.Components.Dashboard"
             d:DataContext="{d:DesignInstance dashboard:DataSourceViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="DATA SOURCE" Style="{StaticResource ModuleTitleStyle}" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Text="{Binding LogPath, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Select log file..."
                        IsReadOnly="True"
                        IsEnabled="{Binding IsInteractionEnabled}"
                        Background="#0a0b10" BorderBrush="{StaticResource BorderBrush}" />
            <ui:Button Grid.Column="1" Command="{Binding SelectLogFileCommand}"
                       IsEnabled="{Binding IsInteractionEnabled}"
                       Icon="{ui:SymbolIcon Folder24}" Margin="8,0,0,0" Padding="8" />
        </Grid>
        <TextBlock Text="Last synced log will be preserved." FontSize="9" Foreground="{StaticResource TextSlateBrush}"
                   Margin="0,8,0,0" Opacity="0.6" />
    </StackPanel>
</UserControl>
