<UserControl x:Class="Komandio.Tools.Mobisync.Components.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dashboard="clr-namespace:Komandio.Tools.Mobisync.Components.Dashboard"
             d:DataContext="{d:DesignInstance dashboard:DashboardViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="800" d:DesignWidth="1200"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Stats Bar -->
        <dashboard:StatsBarView Grid.Row="0" Margin="0,0,0,32"/>

        <!-- Main Dashboard Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,4,0">
                <StackPanel Margin="0,0,8,0">
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,8,16">
                        <dashboard:SessionOverviewView/>
                    </Border>
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,8,16">
                        <dashboard:DataSourceView/>
                    </Border>
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                        <dashboard:ReplayConfigView/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <dashboard:DashboardFeedView x:Name="FeedView"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
