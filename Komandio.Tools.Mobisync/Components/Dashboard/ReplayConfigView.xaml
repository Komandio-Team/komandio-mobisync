<UserControl x:Class="Komandio.Tools.Mobisync.Components.Dashboard.ReplayConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Komandio.Tools.Mobisync.Converters"
             xmlns:dashboard="clr-namespace:Komandio.Tools.Mobisync.Components.Dashboard"
             d:DataContext="{d:DesignInstance dashboard:ReplayConfigViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="REPLAY CONFIGURATION" Style="{StaticResource ModuleTitleStyle}"/>
        
        <CheckBox IsChecked="{Binding ReadFromBeginning, Mode=TwoWay}" 
                  IsEnabled="{Binding IsInteractionEnabled}"
                  Content="START FROM BEGINNING" FontSize="10" FontWeight="Black" Foreground="{StaticResource TextSlateBrush}" Margin="0,0,0,16"/>

        <CheckBox IsChecked="{Binding ShowReplayedLogs, Mode=TwoWay}" 
                  IsEnabled="{Binding IsShowReplayedLogsEnabled}"
                  Content="SHOW REPLAYED LOGS" FontSize="10" FontWeight="Black" Foreground="{StaticResource TextSlateBrush}" Margin="0,0,0,16"/>
    </StackPanel>
</UserControl>