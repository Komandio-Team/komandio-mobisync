<UserControl x:Class="Komandio.Tools.Mobisync.Components.Dashboard.SessionOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shared="clr-namespace:Komandio.Tools.Mobisync.Components.Shared"
             xmlns:dashboard="clr-namespace:Komandio.Tools.Mobisync.Components.Dashboard"
             d:DataContext="{d:DesignInstance dashboard:SessionOverviewViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,24">
            <Border Width="40" Height="40" CornerRadius="20" Background="#1A5e5ce6" BorderBrush="#805e5ce6" BorderThickness="1">
                <TextBlock Text="PI" Foreground="{StaticResource AccentBlueBrush}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="PILOT PROFILE" FontSize="12" FontWeight="Black" Foreground="{StaticResource TextMainBrush}" Typography.Capitals="AllSmallCaps"/>
                <TextBlock Text="{Binding AccountId, StringFormat='UID: {0}'}" FontSize="10" FontFamily="Consolas" Foreground="{StaticResource AccentBlueBrush}"/>
            </StackPanel>
        </StackPanel>

        <!-- Stats Body -->
        <StackPanel Grid.Row="1">
            <shared:SummaryRow Label="HANDLE" Value="{Binding Pilot}" Icon="Person24" Color="{StaticResource AccentBlueBrush}" Margin="0,0,0,6"/>
            <shared:SummaryRow Label="SHARD" Value="{Binding Shard}" Icon="Server24" Margin="0,0,0,6"/>
            <shared:SummaryRow Label="SERVER" Value="{Binding ServerIp}" Icon="Globe24" Margin="0,0,0,6"/>
            <shared:SummaryRow Label="SESSION" Value="{Binding Uptime}" Icon="Clock24" Margin="0,0,0,6"/>
            <shared:SummaryRow Label="GPU" Value="{Binding Gpu}" Icon="TopSpeed24" Color="{StaticResource AccentEmeraldBrush}" Margin="0,0,0,6"/>
            <shared:SummaryRow Label="INPUT" Value="{Binding Input}" Icon="XboxController24" Margin="0,0,0,6"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,24,0,0" Padding="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ENV: " FontSize="8" FontWeight="Black" Foreground="{StaticResource TextSlateBrush}"/>
                    <TextBlock Text="{Binding Env}" FontSize="8" FontWeight="Black" Foreground="{StaticResource TextSlateBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="VER: " FontSize="8" FontWeight="Black" Foreground="{StaticResource TextSlateBrush}"/>
                    <TextBlock Text="{Binding Build}" FontSize="8" FontWeight="Black" Foreground="{StaticResource TextSlateBrush}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>